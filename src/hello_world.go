/**
 * ------------ HELLO WORLD IN GO -------------
 */

// Each Go progam should have exactly one main package with a main function in it
package main

import (
	"fmt" // import the format package
)

// The program execution starts with the main function, so this acts as an access point to the application
func main() {
	fmt.Println("Hello, world!")
}
